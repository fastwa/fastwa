{
  "extends": "../tsconfig.build.json",
  "compilerOptions": {
    "outDir": ".",
    "rootDir": ".",
    "paths": {
      "@playwa/common": ["../common"],
      "@playwa/common/*": ["../common/*"],
      "@playwa/platform-socket": ["../platform-socket"],
      "@playwa/platform-socket/*": ["../platform-socket/*"]
    }
  },
  "exclude": ["node_modules", "dist", "test/**/*", "*.spec.ts"],
  "references": [
    {
      "path": "../common/tsconfig.build.json"
    }
  ]
}