export const S_WHATSAPP_NET = '@s.whatsapp.net';
export const SERVER_ID = 'server@c.us';

export const decodeJid = (jid: string) => {
  return;
};

export const equalsJid = (from: string, to: string) => {
  return decodeJid(from) === decodeJid(to);
};
